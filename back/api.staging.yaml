service: api
runtime: python312
entrypoint: gunicorn -b :$PORT --timeout 600 --threads 4 backend.app:app

env_variables:
  ENV: "PROD"
  WCA_HOST: "https://staging.worldcubeassociation.org"

handlers:
  - url: /.*
    script: auto
    secure: always
