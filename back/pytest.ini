[pytest]
testpaths = tests
# --cov-fail-under is a ratchet: raise it as tests land, never lower it.
addopts =
    --cov=backend
    --cov-report=term-missing:skip-covered
    --cov-report=xml
    --cov-fail-under=53

[coverage:run]
omit =
    backend/load_db/monitoring/*
